export type FormatIterator = {value: number};

export type PrefixContent = [string, string];

export type FormatFlags = {
    '+': boolean,
    '-': boolean,
    ' ': boolean,
    '#': boolean,
    '0': boolean
};
